using Api.Domain.UseCases.FilesMinios.Models;

namespace Api.Domain.UseCases.FilesMinios.Seeders;

public static class FileMinioSeeder
{
    public static void Seed(ModelBuilder modelBuilder)
    {
        // modelBuilder.Entity<FileMinio>().HasData(
        // // new FileMinio { FilMinId = 1, BucketName= "VisionLine", FileName = "ativo", createdAt = DateTime.UtcNow },
        // // new FileMinio { FilMinId = 2, BucketName= "SmartCore", FileName = "ativo", createdAt = DateTime.UtcNow },
        // // new FileMinio { FilMinId = 3, BucketName= "PowerMax", FileName = "ativo", createdAt = DateTime.UtcNow },
        // // new FileMinio { FilMinId = 4, BucketName= "SoundPro", FileName = "ativo", createdAt = DateTime.UtcNow },
        // // new FileMinio { FilMinId = 5, BucketName= "HomeConnect", FileName = "ativo", createdAt = DateTime.UtcNow }
        // );
    }
}